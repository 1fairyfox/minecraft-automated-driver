# 2026-07-24 — Phase 4 Fabric client agent (0.5.0) · Phase 5 Mineflayer lane (0.6.0) · Phase 5 instanced client spawn (0.7.0)

## Done (Phase 5 instanced client spawn — 0.7.0)

- Owner said, verbatim: *"Dont take shortcuts 'the driver-orchestrated instanced
  `client_spawn`' sounds like that."* — so the instance mode deferred in 0.6.0 got built
  for real, no stub.
- `agents/fabric/build.gradle.kts`: new Loom `runProductionClient` task — a real
  production client with our remapped mod + fabric-api + `-Dfairyfox.driver.enable=true`,
  fixed `runDir` (so the agent handshake lands at a known path), XVFB headless. Unlike
  the gametest it stays up until killed.
- `src/client.mjs` — `createClientManager({ jobs, run, platform, exists })`:
  `spawn` deletes any stale handshake, runs the gradle task as a job, polls the run dir
  for the agent's real loopback handshake, **fails fast if the job dies first**, times out
  cleanly otherwise, returns `{ clientId, jobId, runDir, connectDir }`; `list`; `kill`
  (aborts the job → kills the process tree). Wired `client_spawn`/`clients_list`/
  `client_kill` into `src/index.mjs` (default agent dir `agents/fabric`).
- Tests: `test/client.test.mjs` (run + fs faked — the gradle command, the poll-until-
  handshake, fail-fast-on-death, timeout, and kill paths) + protocol tests in
  `test/index.test.mjs`; expected-tools updated. Full gate green on Windows: **107 tests,
  0 fail, c8 99.12% lines / 94.56% branches** (≥90 held).
- Real CI proof: `scripts/client-spawn-smoke.mjs` + a `client-spawn-smoke` job (XVFB,
  PRs into main + dispatch, timeout-capped) — driver spawns a real client → reads its
  handshake → connects over loopback → introspects the title screen → clicks "Options" by
  name → refuses a wrong token → kills it.
- Ledger row 26 (Escalation 9). Released 0.7.0 via release/0.7.0 → PR → checks green →
  tag → back-merge. Added `Client spawn smoke` to main's required checks.

### CI findings — the real first-run flow of a spawned production client (worth keeping)

The spawn smoke failed three times in CI before passing — each a real fact about a
freshly-spawned production client, NOT a driver bug (spawn/handshake/connect succeeded
every time). Recorded so we don't relearn them:

1. **The agent handshake is written during init, before the "Loading Minecraft" splash
   (`class_424`) clears.** So `client_spawn` returning ≠ the UI is drivable yet; a driver
   must poll the semantic screen for readiness, not assume.
2. **A production client runs on intermediary mappings**, so screen *class* names are
   `class_XXX`, not yarn (`TitleScreen`). Key on widget *labels*, never class names.
3. **A fresh run dir opens the "Welcome to Minecraft!" accessibility onboarding
   (`class_8032`) before the title screen** — a real driver clicks "Continue" through it.
4. **The title-screen Options button's label is literally `"Options..."` (trailing
   ellipsis).** Exact-match readiness on "Options" never fires; detect the title screen on
   `"Singleplayer"` (stable, ellipsis-free). The agent's click-by-name still resolves
   "Options" → "Options..." (the gametest proves it).

The smoke now drives the real path: splash → onboarding (Continue) → title (Singleplayer)
→ click Options by name → refuse bad token → kill.

## Done (Phase 5 — 0.6.0)

- Ledger row 25. Built the L2 Mineflayer bot lane (`src/bot.mjs`): a bot registry with an
  injected mineflayer factory (unit-testable without a server) — join (resolve on spawn;
  reject on kick/error/timeout), status, chat, message ring buffer, moveTo (face + hold
  forward until within range), inventory, list, quit. Tools `bot_join`/`bot_status`/
  `bot_chat`/`bot_messages`/`bot_move`/`bot_inventory`/`bots_list`/`bot_quit`;
  `driver_status` → phase 5.
- Tests: bot registry unit-tested (fake EventEmitter bot, incl. graceful degradation when
  the bot lacks vec3/entity/inventory); protocol tests for every `bot_*` tool; c8 gate
  held (98.9% lines / 92% branches). `mineflayer` 4.37.1 + `vec3` 0.1.10 pinned; uuid
  override clears the transitive advisory (0 vulns). **Real smoke** `scripts/bot-smoke.mjs`
  + CI `bot-smoke` job: provision+boot offline Paper → join with a real Mineflayer bot →
  state → chat `/help` → give item + inventory → walk → quit → stop.
- Released 0.6.0 via release/0.6.0 → PR → checks green → tag → back-merge.

## Deferred honestly (S9)

- Driver-orchestrated **instanced client_spawn** (Loom production client) + the
  attach-handshake flow — same heavy Loom infra as the Phase-4 gametest; a focused
  follow-up. `agent_connect kind=fabric` already attaches to a known client dir.
- Fabric agent Java still not in CodeQL (Kotlin + JS are).

## Done (Phase 4 — 0.5.0)

- The Fabric client agent (Java + Loom): gated enable (flag + title-screen opt-in),
  loopback+token control plane, semantic screen introspect + click/key by name. Real
  headless client gametest (XVFB) drives it — after root-causing the "hang" (it was a
  Fabric Loader 0.17.2<0.17.3 crash masked by xvfb-run idling, found in the cancelled
  run's log). See the Phase-4 detail below.