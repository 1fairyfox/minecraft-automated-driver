# 2026-07-24 — Phase 4: Fabric client agent (0.5.0)

## Done

- Ledger row 24. Built the **Fabric client agent** (`agents/fabric/`, Java + Loom — owner
  call: Java matches the sibling's Fabric mod, avoids fabric-language-kotlin):
  - Gating (`EnableGate`, pure + unit-tested): disabled by default; flag
    `-Dfairyfox.driver.enable=true` OR the title-screen "Automated Testing…" button +
    `ConfirmScreen` (process-lifetime only).
  - `ClientControlServer` (loopback+token NDJSON, same protocol) with ops `screen` /
    `click` / `key`, marshalled to the client thread.
  - `WidgetIntrospector` (pure): screen → named widget tree + name resolution
    (exact → unique-substring; ambiguous refused). `Handshake` (pure). `ClientOps`
    (client-coupled: real Screen → descriptors, click by name, keybinding).
  - Driver side: `agent_screen`/`agent_click`/`agent_key`; `agent_connect` gains a
    `kind` (paper `plugins/…` vs fabric `config/…` handshake layout).
- Tests: JaCoCo ≥90 gate on the pure logic (WidgetIntrospector/EnableGate/Handshake);
  a **real headless client gametest** (Fabric client-gametest API + Loom production run +
  XVFB) that boots a rendering client with the agent enabled and drives it over loopback
  (title screen introspect → click "Options" by name → wrong-token refused → screenshots).
  Driver suite extended (33 passing locally on Linux).
- CI: fast `fabric-build` (compile + JaCoCo, cached Loom/MC) every push; heavy
  `fabric-client-gametest` (multi-GB + XVFB) gated to PR→main + dispatch only — the
  owner's CI-cost concern.
- Released 0.5.0 via release/0.5.0 → PR → CI → tag → back-merge.

## Notes / honest gaps (S9)

- The roadmap's full Phase-4 exit ("walk into a server, open a chest by name, screenshot")
  is broader than 0.5.0. Delivered: the crown-jewel *foundation* — title-screen driving by
  name in a real client. Deferred to Phase 4.x/5: in-server movement, container-screen
  driving, a framebuffer `agent_screenshot` op, text entry / direct-connect-by-name.
- Fabric Java not yet in CodeQL (paper Kotlin + JS are) — heavy Loom compile; add when warm.
- Could-not-build-locally: a first Loom build downloads GBs and the 45s bash sandbox can't
  run it to completion, so the Fabric build/gametest are verified in CI (the uncapped Linux
  venue), same as the Paper agent's real proof.
