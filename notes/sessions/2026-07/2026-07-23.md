# 2026-07-23 — Icon live + integrations (0.1.4) · Phase 1 OS layer (0.2.0)

## Done (Phase 1 — 0.2.0)

- Mandate repeated with the backlog clear → execution moved into the roadmap
  (ledger row 19). Roadmap amendment recorded: job model → Phase 2 (first real
  consumers; no-dead-code).
- Built the L0 OS layer: `src/config.mjs`, `src/instances.mjs`, `src/os/windows.mjs`
  (window enumeration, PrintWindow/screen screenshots, graceful/forced close,
  detached spawn — PowerShell runner, spawner, platform all injectable). Tool
  surface: `os_windows_list`, `os_screenshot` (image content), `instance_open`,
  `instance_close`, `instances_list`; `driver_status` → phase 1.
- 54 tests, gate held (100/100/100 + ~99% branches; two defensive dead branches
  removed rather than tolerated). **Real smoke passed on this machine**: notepad
  spawned, window found (1280×665, 27 KB PNG), closed. Same smoke added as a
  `windows-latest` CI job and to `main`'s required checks.
- Released 0.2.0 the MINOR git-flow way: `release/0.2.0` branch → PR → all checks
  green → merge → tag → back-merge → release branch deleted.

## Done (earlier: icon + integrations — 0.1.4)

- **Project icon live** (owner art at `assets/icon.png`): docs-site favicon (hard
  build requirement + not-the-placeholder regression test) and README header image.
- **Keys question answered from evidence**: the sibling repo carries three secrets —
  CODECOV_TOKEN, SONAR_TOKEN, SCORECARD_TOKEN (`gh secret list`). Tokenless limits
  confirmed: Codecov tokenless = rate-limited; Sonar CI-scan = token required
  (Automatic Analysis is the tokenless path but imports no coverage); Scorecard
  tokenless = Branch-Protection check scores 0.
- **Integrations mirrored, token-gated** (sibling's own pattern): Codecov upload
  (c8 lcov, non-blocking) + Sonar scan (`if: env.SONAR_TOKEN != ''`) in ci.yml;
  `codecov.yml`; `sonar-project.properties` (JS lcov import, keys per the sibling's
  minted-org convention with a confirm-after-import warning).
- Released 0.1.4 via PR with the full gate green; live icon verified on the site.

## Keys landed + services verified (later)

- Owner set all three secrets (one Codecov mispaste, re-run, corrected). Verified end
  to end from CI logs: Codecov upload **queued/accepted** for commit 0655072;
  SonarCloud **ANALYSIS SUCCESSFUL** → dashboard `1fairyfox_minecraft-automated-driver`
  (minted keys matched the convention — no properties fix needed); Scorecard run green
  with the PAT.
- **First published Scorecard score: 6.9** — 0.1 under floor, wholly time-gated
  (Maintained=0 under 90 days; Code-Review=0 solo-structural). All actionable checks
  at 10. Enabled `dismiss_stale_reviews` on main (last cheap Branch-Protection
  criterion that doesn't require approvers; the protection change auto-triggers a
  re-scan). Floor recovery is a function of repo age; re-check at the weekly scans.
- Remaining Scorecard headroom identified for Phase 7: a release workflow shipping
  artifacts + provenance turns Signed-Releases/Packaging from excluded to scoring.

## Owner steps queued (ledger row 18)

`gh secret set CODECOV_TOKEN / SONAR_TOKEN / SCORECARD_TOKEN -R
1fairyfox/minecraft-automated-driver`; import the repo on Codecov and SonarCloud
(disable Automatic Analysis there if the CI scan should own it); correct
sonar-project.properties keys if SonarCloud mints different ones.
