# 2026-07-22 — Project founding

## Done (later: rename + first release + GitHub)

- **Renamed** PaperMC Automated Driver → **Minecraft Automated Driver** (owner call —
  not Paper-specific; mandate ledger `plans/mandate-2026-07-22-rename.md`). All
  identity uses updated; Paper-the-platform references untouched. VERSION → 0.1.1.
- **Released** the git-flow way: rename commit on `dev`, `--no-ff` merge to `main`,
  tags v0.1.0 (founding) + v0.1.1, back-merge. Full local gate green first
  (npm test, 0 audit findings, check-links, check-tidy, stale-name grep = 0 hits).
- **GitHub repo created + pushed**: `1fairyfox/minecraft-automated-driver` — unblocks
  the owner's hub registration (owner handles hub paperwork + group rename).

## Done (founding)

- **Project conceived and split out** from despawned-items: the accumulated automation
  layers (Mineflayer smoke, screenshot harness, playtest bootstrap, client mod, CI
  server-boot jobs) get unified behind one MCP server, generalized for any project.
- **Owner decisions captured** (see `decisions/architecture.md`): JS-not-TS host; Paper
  server + Fabric client first wave; instance AND attach modes both first-class;
  flag-gated + in-game-opt-in agent enablement; plan + full scaffold as the deliverable.
- **Research pass:** MCP JS SDK stable = 1.29.0, v2 split lands 2026-07-28 (build on
  v1); Fabric's official client gametest API covers input simulation, screenshots
  (incl. golden-image compare), dedicated-server contexts, headless CI via Loom
  production run tasks + XVFB — the agent mod becomes its interactive counterpart;
  current MC line is 26.1.x but tooling (Mineflayer/MockBukkit) holds the target at
  1.21.11.
- **Scaffolded the repo**: mesh standards seeded from the sibling node (reference
  standards copied minus its project-specific test lore), notes tree seeded fresh,
  CLAUDE.md/README/SECURITY.md written for this project, working MCP skeleton
  (`driver_status` + test), dependabot (npm + actions only — no dead gradle config).
- **Founding roadmap written**: `plans/roadmap-2026-07.md` — layer model, security
  model, Phases 0–8.
- First commit on `dev` (see `version/2026-07.md` → 0.1.0).

## Notes / rough edges

- Standards came from the sibling node, not the hub — its modifications aren't
  upstreamed yet, so the first fairyfox check-for-updates will show divergence both
  ways. Flagged in CLAUDE.md provenance note + the process report.
- CI/docs-site/hub-registration deliberately deferred (Phase 7) and recorded as
  honest gaps in `status.md`.
